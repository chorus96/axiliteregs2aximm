# A more detailed description of the manifest format `Bender.yml` can be found here:
# https://github.com/pulp-platform/bender#manifest-format-benderyml

package:
  name: axiliteregs2aximm
  authors:
    - "Codex <codex@openai.com>"

dependencies:
  reg2axi: { git: "https://github.com/chorus96/reg2axi.git", version: 1.0.6 }
  axi: { git: "https://github.com/chorus96/axi.git", version: 0.40.0 }

sources:
  # Source files grouped in levels. Files in level 0 have no dependencies on files in this
  # package. Files in level 1 only depend on files in level 0, files in level 2 on files in
  # levels 1 and 0, etc. Files within a level are ordered alphabetically.
  # Level 0
